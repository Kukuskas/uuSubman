

function name(params) {
    if (guest) return only topic names
    if (student) return <bricks.icon of study material content= blabla> </bricks.icon of study material>
}
function allTopics(params) {
        const books=[]
        const courses=[]
        const videos=[]
        const others=[]
        for...
        <bricks.acordeon content ={name(x)}> </bricks.acordeon>
}
    render {
        return
        <div>
            {allTopics(subject.language.studyForm.topic)}
        </div>
    }